<div data-page="alerts" class="demo-page-content">

    {{> demo-sidebar }}

    <div class="demo-container-with-sidebar">
        <header class="content-block content-block--strip bg-primary">
            <div class="container">
                <h2>Alerts</h2>
            </div>
        </header>


        <!-- REQUIRED FILES -->
        <section id="alertsRequired">
            <header class="content-block bg-light-grey">
                <div class="container">
                    <h3>Required files</h3>
                </div>
            </header>
            <div class="content-block">
                <div class="container">
                    <p>SCSS files required: <code>_components.alerts.scss</code></p>
                </div>
            </div>
        </section>


        <section id="alertsMarkup">
            <header class="content-block bg-light-grey">
                <div class="container">
                    <h3>Alert message boxes and variations</h3>
                    <p>To create an alert message block, use the class <code>.alert</code> on a div and then add the modifying classes to state which kind of alert message <code>alert--danger</code>, <code>alert--warning</code>, <code>alert--success</code>, <code>alert--info</code></p>
                    <p>The alert messages are full width with a 10px bottom margin</p>
                    <p>A mixin is also available to create an alert message colour scheme <a href="sass-tools.html#toolsAlerts">here</a></p>
                </div>
            </header>

            <div class="content-block">
                <div class="container">
                    <div class="alert alert--danger">
                        This is an error alert message.
                    </div>

                    <div class="alert alert--warning">
                        This is a warning alert message.
                    </div>

                    <div class="alert alert--success">
                        This is a success alert message.
                    </div>

                    <div class="alert alert--info">
                        This is an info alert message.
                    </div>
                </div>
            </div>
        </section>
    </div>
</div>
